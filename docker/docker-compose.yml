version: '3'
services:
  db:
    image: "mongo:4.0"
    ports:
      - "27017:27017"
  api: 
    build: .
    volumes: 
      - ../api/node:/var:rw
    ports:
      - "3000:3000"
    environment:
      - DB_HOST=db
      - DB_PORT=27017
      - PORT=3000
    depends_on:
      - db
 